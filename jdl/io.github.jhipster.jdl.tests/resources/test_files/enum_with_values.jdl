enum EnvironmentType {
  ARCHIVE (archive),
  DEV (development),
  INTEGRATION (integration),
  PROD (production),
  TEST (test),
  UAT (uat),
  NON_PROD (nonProd),
  STRING_VALUE("This is an enum too")
}

entity Environment {
  type EnvironmentType required
}
